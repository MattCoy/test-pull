<!doctype html>
<html>
<head>
    <meta charset="utf-8" />
    <link href="{{ asset('assets/bootstrap/css/bootstrap.min.css') }}" rel="stylesheet" />
    <title>Silex - list</title>
</head>
<body>
    <div class="container">
        <header>
            <h1>Silex</h1>
        </header>
        <a href="{{ path('homepage') }}">Home</a>
        <div class="col-md-8">
            {% for article in articles %}
            <article>
                <h2>{{ article.title }}</h2>
                <p>publié par <a href="{{ path('author', {id:article.author.id}) }}">{{ article.author.name }}</a></p>
                <p> le {{ article.date_publi }}</p>
            </article>
            {% endfor %}
        </div>
        <footer class="footer">
        </footer>
    </div>
</body>
</html>